## Bigram, Trigram Extraction from product description

The following project extracts features as bigrams/trigrams from the description for some applications.
First, we pre process the raw description and then extract the bigrams and trigrams. Finally we compare the results between the manually extracted features and the bi/trigrams.

Python's nltk lib was used to perform NLP tasks.
